import { getMethod } from "../../helpers"

const API_URL_LOGIN = "users/login"
const API_URL_REGISTER = "vendors"
const API_URL_FORGOTPASSWORD = "users/send-otp"
const API_URL_RESETPASSWORD = "users/forgot-password"


const vendorLogin = async()=>{
    const response = await getMethod()
}